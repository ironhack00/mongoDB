use("platzi_store");

/* db.inventory.find(
    {
        'item.description': { $regex: /^single/i  }
    }
); */

db.inventory.find(
    //para realizar busquedas con saltos de linea
    {
        'item.description': { $regex: /^s/im  }
    }
);